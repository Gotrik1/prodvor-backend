# Этот файл является шаблоном для переменных окружения. 
# Скопируйте его в файл .env и заполните реальными значениями. 
# Файл .env никогда не должен попадать в систему контроля версий (Git).

# --- Подключение к базе данных ---
# Полный URL-адрес для подключения к вашей базе данных PostgreSQL.
# Формат: postgresql://[пользователь]:[пароль]@[хост]:[порт]/[имя_базы_данных]
DATABASE_URL=postgresql://user:password@host:port/dbname

# --- Интеграция с Supabase ---
# URL вашего проекта в Supabase.
SUPABASE_URL=

# Ключ доступа (service_role) для вашего проекта Supabase.
# Этот ключ позволяет обходить любые политики безопасности на уровне строк (RLS), 
# поэтому обращайтесь с ним как с паролем. Храните его в секрете.
SUPABASE_KEY=

# --- Ключи безопасности Flask и JWT ---
# Секретный ключ, используемый Flask для подписи сессий и других данных, 
# связанных с безопасностью. Должен быть длинной, случайной строкой.
SECRET_KEY=

# Отдельный секретный ключ для подписи JWT-токенов обновления (refresh tokens).
# Использование отдельного ключа повышает безопасность.
REFRESH_SECRET_KEY=
